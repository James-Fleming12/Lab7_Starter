Lab 7 - James Fleming

1. We'd fit them within a github action that runs whenever code is pushed to follow the principles of CI. This way whenever code is pushed, the tests run automatically, reducing the amount of user error that could occur.

2. No

3. Navigation mode analyzes the page right as it is loading (providing overall performance metrics) and Snapshot mode analyzes it in its current state (good for dynamic elements).

4. We could add a language attribute for accessibility, add a viewport meta tag for mobile-friendly practices, and adding a meta description for the site could improve SEO